{
  "name": "drizzle-tx-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc -b tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit && pnpm -r run typecheck",
    "test": "vitest run",
    "test:unit": "vitest run --project core-unit",
    "test:int": "vitest run --project core-integration",
    "test:nestjs": "vitest run --project nestjs-unit --project nestjs-integration",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage",
    "coverage:unit": "vitest run --project core-unit --project nestjs-unit --coverage",
    "coverage:ui": "vitest --ui --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "prepare": "husky",
    "check:publish": "pnpm -r run check:publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11",
    "@changesets/cli": "^2.31.0",
    "@testcontainers/postgresql": "catalog:",
    "@types/node": "^26.4.0",
    "@types/pg": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@vitest/ui": "catalog:",
    "drizzle-kit": "catalog:",
    "drizzle-orm": "catalog:",
    "husky": "^9.1.7",
    "pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
